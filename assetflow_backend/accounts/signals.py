# Reserved for account-related signal handlers (e.g. welcome notification
# on signup). Kept separate so accounts.apps.AccountsConfig.ready() always
# has a module to import without raising on a clean checkout.
